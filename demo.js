const addbuton = document.querySelector("#add")

const AddNewNote = () =>{

    const note = document.createElement(div);
    note.classList.add("notes")

    const addHtml = `<div> chandigarh </div>`
    



}

addbuton.addEventListener("onclick", AddNewNote());